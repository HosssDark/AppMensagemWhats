namespace Functions
{
    public static class Enum
    {
        public enum TypeMessage
        {
            Success,
            Error,
            Info,
            Warning
        }

        public enum TypeResult
        {
            Ok,
            Error
        }
    }
}
